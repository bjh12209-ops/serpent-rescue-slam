rtabmap:
  ros__parameters:
    frame_id: base_link
    odom_frame_id: odom
    map_frame_id: map
    publish_tf: true
    wait_for_transform: 0.3

    subscribe_depth: true
    subscribe_rgbd: false
    subscribe_rgb: false
    subscribe_stereo: false
    subscribe_scan: false
    subscribe_scan_cloud: false
    subscribe_odom_info: false

    # Odometry is read from odom -> base_link TF. D435 color and aligned depth
    # timestamps are close but are not guaranteed to be exactly identical.
    approx_sync: true
    approx_sync_max_interval: 0.02
    topic_queue_size: 3
    sync_queue_size: 5
    qos_image: 2
    qos_camera_info: 2
    qos_imu: 1

    Reg/Strategy: "0"
    Optimizer/GravitySigma: "0.3"
    # The camera and odometry stay at 30 Hz. Process online map/loop-closure
    # Measured map updates reached 0.3-1.9 seconds on this mini PC. A 5 Hz
    # request therefore built a permanent backlog. Two graph updates per
    # second keep camera/VO live while remaining adequate for online mapping.
    Rtabmap/DetectionRate: "2.0"
    Rtabmap/TimeThr: "450"
    Kp/MaxFeatures: "700"
    Vis/MaxFeatures: "1000"
    Vis/MinInliers: "20"
    RGBD/LinearUpdate: "0.05"
    RGBD/AngularUpdate: "0.05"
    RGBD/OptimizeFromGraphEnd: "false"

    # Keep a 3 cm 3-D occupancy representation for navigation/debugging.
    # The database also retains RGB-D data used by rtabmap-export for meshes.
    Grid/FromDepth: "true"
    Grid/3D: "true"
    Grid/CellSize: "0.03"
    Grid/DepthDecimation: "2"
    Grid/RangeMax: "5.0"
    Grid/RayTracing: "false"
